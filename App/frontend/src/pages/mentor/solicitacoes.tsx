import { PagePlaceholder } from '@/components/page-placeholder'

export function MentorSolicitacoesPage() {
  return <PagePlaceholder titulo="Solicitações" descricao="Solicitações de mentoria pendentes." />
}
