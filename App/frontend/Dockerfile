# ---------- Etapa 1: build do SPA (Vite + React) ----------
FROM node:22-alpine AS build

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

# Em produção o SPA chama a API pela mesma origem (proxy do nginx), por isso a URL relativa
ARG VITE_API_URL=/api/v1
ENV VITE_API_URL=$VITE_API_URL

RUN npm run build

# ---------- Etapa 2: produção (nginx servindo o build estático) ----------
FROM nginx:alpine

COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
